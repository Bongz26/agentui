{
  "name": "fieldlink",
  "version": "1.0.0",
  "description": "FieldLink Field Agent Digital Onboarding Platform",
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install --include=dev && cd ../client && npm install --include=dev",
    "seed": "cd server && npm run seed",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "start cmd /c \"cd server && npm run dev\" && cd client && npm run dev",
    "build": "npm run install:all && cd client && npm run build",
    "start": "cd server && npm start"
  },
  "workspaces": [
    "server",
    "client"
  ]
}
